import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { html } from "lit-html";
import { unsafeSVG } from "lit-html/directives/unsafe-svg.js";
import FCarouselExample from "./svg/i-fcarousel-example.js";

<Meta
	title="Components/f-carousel"
	parameters={{
		controls: { hideNoControlsWarning: true }
	}}
/>

# f-div

<br />
<br />

## Overview

<hr />

f-div is used as a container for HTML elements.

<br />

### [Playground](/story/components-f-div--playground)

<Canvas>
	<Story name="Playground">
		{args => {
			const handleNext = e => {
				console.log("in next callback", e);
			};
			const handlePrev = () => {
				console.log("in previous callback", e);
			};
			return html`<f-div width="100%">
				<f-carousel auto-play active-content-id="slide-3" @next=${handleNext} @prev=${handlePrev}>
					${[1, 2, 3, 4, 5].map(
						idx =>
							html`<f-carousel-content content-id=${"slide-" + idx}>
								<f-div> ${unsafeSVG(FCarouselExample)} </f-div>
								<f-text>${"slide-" + idx}</f-text>
							</f-carousel-content>`
					)}
				</f-carousel>
				<f-carousel active-content-id="slide-primary">
					${["secondary", "tertiary", "primary", "danger", "warning"].map(
						idx =>
							html`<f-carousel-content content-id=${"slide-" + idx}>
								<f-div
									height="300px"
									width="100%"
									padding="large"
									gap="large"
									variant="curved"
									.state=${idx}
								>
									<f-text state="inherit">${idx}</f-text>
									<f-icon source="i-crown" state="inherit"></f-icon>
								</f-div>
							</f-carousel-content>`
					)}
				</f-carousel>
			</f-div>`;
		}}
	</Story>
</Canvas>
