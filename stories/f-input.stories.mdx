import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { html } from "lit-html";
import fSpacerAnatomy from "./svg/i-fspacer-anatomy.js";
import { unsafeSVG } from "lit-html/directives/unsafe-svg.js";
import { useArgs, useEffect, useState } from "@storybook/client-api";

<Meta
  title="Components/f-input"
  parameters={{
    controls: { hideNoControlsWarning: true },
  }}
/>

# f-spacer

<br />
<br />

## Overview

<f-divider></f-divider>

f-spacer is used to create a pseudo space in the layout.

<br />

### [Playground](/story/components-f-input--playground)

<Canvas>
  <Story
    name="Playground"
    argTypes={{
      value: { control: "text" },
      placeholder: { control: "text" },
      variant: {
        control: "select",
        options: ["curved", "round", "block"],
      },
      category: {
        control: "select",
        options: ["fill", "transparent", "outline"],
      },
      type: {
        control: "select",
        options: ["text", "number", "email", "password", "url", "tel"],
      },
      ["icon-left"]: {
        control: "text",
      },
      ["icon-right"]: {
        control: "text",
      },
      ["prefix"]: {
        control: "text",
      },
      ["suffix"]: {
        control: "text",
      },
      loading: {
        control: "boolean",
      },
      disabled: {
        control: "boolean",
      },
      ["group-direction"]: {
        control: "radio",
        options: ["vertical", "horizontal"],
      },
      ["group-gap"]: {
        control: "select",
        options: ["large", "medium", "small", "x-small"],
      },
      ["element-state"]: {
        control: "select",
        options: ["default", "success", "primary", "warning", "danger"],
      },
      ["form-size"]: {
        control: "radio",
        options: ["small", "medium"],
      },
      ["textarea-value"]: {
        control: "text",
      },
    }}
    args={{
      value: "",
      placeholder: "Enter your text here",
      variant: "round",
      category: "fill",
      type: "text",
      ["icon-left"]: undefined,
      ["icon-right"]: undefined,
      ["prefix"]: undefined,
      ["suffix"]: undefined,
      loading: false,
      disabled: false,
      ["group-direction"]: "vertical",
      ["group-gap"]: "large",
      ["element-state"]: "default",
      ["form-size"]: "small",
      ["textarea-value"]: "",
    }}
  >
    {(args) => {
      const [_, updateArgs] = useArgs();
      const [checked, setChecked] = useState("unchecked");
      const [radioChecked, setRadioChecked] = useState("unselected");
      const [switchValue, setSwitchValue] = useState("off");
      const handleInput = (e) => {
        updateArgs({ value: e.detail.value });
      };
      const handleInputTextArea = (e) => {
        updateArgs({ ["textarea-value"]: e.detail.value });
      };
      const handleChecked = (e) => {
        setChecked(e.detail.value);
      };
      const handleRadioChecked = (e) => {
        setRadioChecked(e.detail.value);
      };
      const handleSwitch = (e) => {
        setSwitchValue(e.detail.value);
      };
      return html`
        <f-form size=${args["form-size"]}>
          <f-form-group direction=${args["group-direction"]} gap=${args["group-gap"]}>
            <f-field
              state=${args["element-state"]}
              required=${true}
              .disabled=${args.disabled}
              .loading=${args.loading}
            >
              <f-text variant="para" size="small">Label *</f-text>
              <f-input
                .value=${args.value}
                @update=${handleInput}
                .placeholder=${args.placeholder}
                .variant=${args.variant}
                .category=${args.category}
                .type=${args.type}
                icon-left=${args["icon-left"]}
                icon-right=${args["icon-right"]}
                prefix=${args["prefix"]}
                suffix=${args["suffix"]}
                clear=${true}
                size="medium"
              ></f-input>
            </f-field>
            <f-field state=${args["element-state"]}>
              <f-text variant="para" size="small">Label *</f-text>
              <f-input
                value=${args.value}
                .placeholder=${args.placeholder}
                @update=${handleInput}
                .variant=${args.variant}
                .category=${args.category}
                .type=${args.type}
                icon-left=${args["icon-left"]}
                icon-right=${args["icon-right"]}
                prefix=${args["prefix"]}
                suffix=${args["suffix"]}
              ></f-input>
            </f-field>
            <f-field state=${args["element-state"]}>
              <f-text variant="para" size="small">Label *</f-text>
              <f-input
                value=${args.value}
                .placeholder=${args.placeholder}
                @update=${handleInput}
                .variant=${args.variant}
                .category=${args.category}
                .type=${args.type}
                icon-left=${args["icon-left"]}
                icon-right=${args["icon-right"]}
                prefix=${args["prefix"]}
                suffix=${args["suffix"]}
              ></f-input>
            </f-field>
            <f-div padding="small" state="tertiary" clickable variant="curved">
              <f-field type="checkbox" state=${args["element-state"]}>
                <f-checkbox value=${checked} @update=${handleChecked}></f-checkbox>
                <f-div padding="none" gap="x-small" direction="column" align="middle-left">
                  <f-text
                    variant="para"
                    size="small"
                    @click=${() => {
                      if (checked === "checked") {
                        setChecked("unchecked");
                      } else {
                        setChecked("checked");
                      }
                    }}
                    >Label (value="${checked}") *</f-text
                  >
                  <f-text variant="para" size="small" state="secondary"
                    >States are used to communicate purpose and connotations. For example, a red
                    color connotes danger, whereas a green color connotes success and so on.
                  </f-text>
                </f-div>
              </f-field>
            </f-div>
            <f-div padding="small" state="tertiary" clickable variant="curved">
              <f-field type="radio" state=${args["element-state"]}>
                <f-radio value=${radioChecked} @update=${handleRadioChecked}></f-radio>
                <f-div padding="none" gap="x-small" direction="column" align="middle-left">
                  <f-text
                    variant="para"
                    size="small"
                    @click=${() => {
                      if (radioChecked === "selected") {
                        setRadioChecked("unselected");
                      } else {
                        setRadioChecked("selected");
                      }
                    }}
                    >Label (value="${radioChecked}") *</f-text
                  >
                  <f-text variant="para" size="small" state="secondary"
                    >States are used to communicate purpose and connotations. For example, a red
                    color connotes danger, whereas a green color connotes success and so on.
                  </f-text>
                </f-div>
              </f-field>
            </f-div>
            <f-field type="checkbox" state=${args["element-state"]}>
              <f-checkbox size="small" value="indeterminate"></f-checkbox>
              <f-div padding="none" align="middle-center">
                <f-text variant="para" size="small">Banana</f-text>
              </f-div>
            </f-field>
            <f-field type="radio" state=${args["element-state"]}>
              <f-radio></f-radio>
              <f-div padding="none" align="middle-center">
                <f-text variant="para" size="large">Banana</f-text>
              </f-div>
            </f-field>
            <f-field type="switch" state=${args["element-state"]} .disabled=${args.disabled}>
              <f-switch value=${switchValue} @update=${handleSwitch}></f-switch>
            </f-field>
            <f-field state=${args["element-state"]}>
              <f-div height="200px" width="500px" padding="none" overflow="hidden">
                <f-textarea
                  variant="curved-fill"
                  placeholder="This is a Text Area"
                  resizable=${true}
                  clear=${true}
                  state=${args["element-state"]}
                  value=${args["textarea-value"]}
                  @update=${handleInputTextArea}
                ></f-textarea>
              </f-div>
            </f-field>
          </f-form-group>
          <f-form-group direction="vertical" gap=${args["group-gap"]}>
            <f-text variant="heading" size="small">Favourite Fruit</f-text>
            <f-field type="checkbox" state=${args["element-state"]}>
              <f-checkbox value=${checked} @update=${handleChecked}></f-checkbox>
              <f-div padding="none" align="middle-center">
                <f-text variant="para" size="small">Apple</f-text>
              </f-div>
            </f-field>
            <f-field type="checkbox" state=${args["element-state"]}>
              <f-checkbox value=${checked} @update=${handleChecked}></f-checkbox>
              <f-div padding="none" align="middle-center">
                <f-text variant="para" size="small">Banana</f-text>
              </f-div>
            </f-field>
            <f-field type="checkbox" state=${args["element-state"]}>
              <f-checkbox value=${checked} @update=${handleChecked}></f-checkbox>
              <f-div padding="none" align="middle-center">
                <f-text variant="para" size="small">Orange</f-text>
              </f-div>
            </f-field>
          </f-form-group>
        </f-form>
      `;
    }}
  </Story>
</Canvas>
