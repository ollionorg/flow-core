import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { html } from "lit-html";
import fDividerAnatomy from "./svg/i-fdivider-anatomy.js";
import { unsafeSVG } from "lit-html/directives/unsafe-svg.js";
import samplelogs from "./logs/logs.js";
import { useArgs, useEffect, useState } from "@storybook/client-api";

<Meta
	title="@cldcvr/flow-log/Components/f-log"
	parameters={{
		controls: { hideNoControlsWarning: true }
	}}
/>

# f-log

<br />
<br />

## Overview

<f-divider></f-divider>

f-log is used for terminal purpose.

<br />

### [Playground](/story/cldcvr-flow-log-components-f-log--playground)

<Canvas>
	<Story
		name="Playground"
		argTypes={{
			logs: { control: "text" },
			["show-search"]: { control: "boolean" },
			["show-scrollbar"]: { control: "boolean" }
		}}
		args={{
			logs: samplelogs,
			["show-search"]: false,
			["show-scrollbar"]: false
		}}
	>
		{args => {
			const [_, updateArgs] = useArgs();
			const handleClose = e => {
				console.log(e.detail.value);
				const handleInput = e => {
					updateArgs({ ["show-search"]: e.detail.value });
				};
			};
			return html`<f-div direction="column" padding="x-large" height="100%"
				><f-log
					.logs=${args.logs}
					?show-search=${args["show-search"]}
					?show-scrollbar=${args["show-scrollbar"]}
					@close=${handleClose}
				></f-log
			></f-div>`;
		}}
	</Story>
</Canvas>

<br />

## logs

<f-divider></f-divider>

logs is the stringied form of data to be inserted into the terminal `f-log`

<Canvas>
	<Story name="logs">
		{args => {
			return html`
				<f-div direction="column" padding="x-large" height="100%"
					><f-log .logs=${samplelogs}></f-log
				></f-div>
			`;
		}}
	</Story>
</Canvas>

<br />

## show-search

<f-divider></f-divider>

f-log would have search box to find any key name

<Canvas>
	<Story name="show-search">
		{args => {
			return html`
				<f-div direction="column" padding="x-large" height="100%"
					><f-log ?show-search=${true} .logs=${samplelogs}></f-log
				></f-div>
			`;
		}}
	</Story>
</Canvas>

<br />

## show-scrollbar

<f-divider></f-divider>

f-log would have scrollbar

<Canvas>
	<Story name="show-scrollbar">
		{args => {
			return html`
				<f-div direction="column" padding="x-large" height="100%"
					><f-log ?show-scrollbar=${true} .logs=${samplelogs}></f-log
				></f-div>
			`;
		}}
	</Story>
</Canvas>
